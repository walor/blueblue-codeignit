<%=render 'sidebar'%>
<div class="span9">
    <h3 class='text-center'>Lista de areas</h3>

    <p id="alert"><%= alert %></p>

    <table class="table table-striped table-hover table-condensed">
      <tr>
        <th>Código</th>
        <th>Nombre</th>
        <th>Activo</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

    <% @areas.each do |area| %>
      <tr>
        <td><%= area.code %></td>
        <td><%= area.name %></td>
        <td><%= t(area.active.to_s) %></td>
        <td><%= show_link area %></td>
        <td><%= edit_link edit_area_path(area) %></td>
        <td><%= destroy_link area %></td>
      </tr>
    <% end %>
    </table>
</div>